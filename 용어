boolean: 논리값을 저장하는 저장공간
문자열 : string 키워드
배열 array : 연속된 메모리 공간을 나타내는 자료구조
슬라이스 : 가변적인 길이의 배열을 말한다.
구조체 : 필드(변수)의 집합 자료구조
포인터 : 메모리 주소를 값으로 갖는 타입
함수 타입 : 함수를 가리키는 타입
인터페이스 : 매서드 정의의 집합
맵 : 키 값을 갖는 데이터를 저장해둔 자료구조
채널 : 멀티스레드 환경에 특화된 큐 형태의 자료구조

for문 요약

for 초기값; 조건문; 증감{
 ..   
}

for 조건문{    for {   // 반복         for i,v := range 변수값{
 ..          
}              }                       }


if 함수; 조건값{        if 조건문 {

}                       }

var a [5]int   // 변수선언           a := [5]int{ 4, 6, 23, 5}  //선언과 동시에 초기화

type st struct{              // 구조체 정의       var 명칭 구조체명
   명칭 타입                                      st1.객체명칭
}

var a *int = st     // 포인터 선언           func test(a int, b *int) *int{       대입에 포인터 선언시
a := &st{}                 *a 가르킨 주소의 값                                    주소 받아옴   반환에
var a = new(st)             a 가르키는 주소값                                     넣을시 주소 반환

string 중에 값을 중간에 값을 추가할수없는 불변이다.
이유는 오류를 방지하기 위해서

strings 패키지의 builder는 가변적으로 선언이 가능하다 내부에 []rune 슬라이스가 있다.

 var builder(명칭) string.builder 객체 를 통해 선언가능


